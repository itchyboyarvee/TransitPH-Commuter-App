<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">
        <TextView style="@style/TransitEyebrow" android:text="Saved trips" />
        <TextView style="@style/TransitPageTitle" android:text="Your usual rides, one tap away." />
        <TextView android:id="@+id/saved_status" style="@style/TransitBody" android:layout_marginTop="12dp" android:text="Sign in to see private saved routes." />
        <Button android:id="@+id/saved_action" style="@style/TransitButton" android:layout_marginTop="18dp" android:text="Sign in  →" />
        <LinearLayout android:id="@+id/saved_list" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="12dp" android:orientation="vertical" />
    </LinearLayout>
</ScrollView>